"""LM helpers with mixed boundary maturity.

``dr_dspy.lm.boundary`` is the forward prompt/provider boundary for new
graph-platform work. The DSPy runner, signature, and OpenRouter wrapper modules
remain legacy compatibility surfaces for v0 workflows.
"""
