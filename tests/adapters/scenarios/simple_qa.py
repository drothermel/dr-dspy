from tests.task_spec.helpers import ts

SIMPLE_QA_SIGNATURE = ts("question -> answer", instructions="Given the fields, produce the outputs.")
SIMPLE_QA_INPUTS = {"question": "What is the capital of France?"}
